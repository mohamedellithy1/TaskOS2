on:
  push:
    branches:
      - master










jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      
      
      
      - uses: actions/checkout@v3
      - name: "Login to Docker"
        run: docker login --username mohamedeee10 --password dckr_pat_mKWUdup8OgfeHBhjXJR8OorR7tI

      
      

      